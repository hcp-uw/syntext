{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "cd .. && docker-compose -f docker-compose.test.yaml up --build",
    "jest": "NODE_ENV=test jest --verbose --runInBand",
    "integration-test": "(npm run start&) && npm run localDBinit && npm run jest",
    "start": "nodemon server",
    "start-init": "node server && npm run localDBinit",
    "dev": "nodemon devserver.js",
    "localDBinit": "node ./db-admin-scripts/create-tables && node ./db-admin-scripts/clear-tables && node ./db-admin-scripts/fill-tables"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "axios": "^1.3.4",
    "jest": "^29.5.0",
    "nodemon": "^2.0.20"
  },
  "dependencies": {
    "body-parser": "^1.20.1",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mysql2": "^3.1.2",
    "path": "^0.12.7"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
